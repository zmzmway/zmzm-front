export const SLOT = {
  PROVIDER: "tooltip-provider",
  ROOT: "tooltip",
  TRIGGER: "tooltip-trigger",
  CONTENT: "tooltip-content",
} as const;

export const DEFAULT = {
  DELAY_DURATION: 0,
  SIDE_OFFSET: 0,
} as const;
